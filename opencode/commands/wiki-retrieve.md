---
name: wiki-retrieve
description: Build and query a vault-local contextual BM25 retrieval index with semantic search
---

# /wiki-retrieve

Perform hybrid BM25 and contextual semantic passage retrieval across the local Obsidian vault chunks.

Target/Arguments: $ARGUMENTS

If the `wiki-retrieve` skill is available, invoke its guidelines and tools before proceeding.
