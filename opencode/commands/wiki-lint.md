---
name: wiki-lint
description: Run a deterministic, read-only health check on an Obsidian wiki (broken links, orphans, ledgers)
---

# /wiki-lint

Execute deterministic vault health validation: check for broken wikilinks, orphan notes, frontmatter validity, and ledger integrity.

Target/Arguments: $ARGUMENTS

If the `wiki-lint` skill is available, invoke its guidelines and tools before proceeding.
