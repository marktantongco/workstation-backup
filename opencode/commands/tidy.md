---
name: tidy
description: Hunt complexity in working tree diff and apply simplifications directly
---

# /tidy

Analyze current git diff for unnecessary complexity, duplicate code, or performance bloat, and apply clean refactorings directly.

Target/Arguments: $ARGUMENTS

If the `tidy` skill is available, invoke its guidelines and tools before proceeding.
