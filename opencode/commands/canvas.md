---
name: canvas
description: Create, inspect, and update Obsidian JSON Canvas boards with text, file, link, and edge nodes
---

# /canvas

Inspect, design, or modify an Obsidian JSON Canvas (.canvas) board with visual nodes, groups, and semantic edge relationships.

Target/Arguments: $ARGUMENTS

If the `canvas` skill is available, invoke its guidelines and tools before proceeding.
